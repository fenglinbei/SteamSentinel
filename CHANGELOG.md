# Changelog

## 0.1.10 - 2026-09-04

- 新增蓝色盾牌与修复扳手应用图标，接入程序、扫描和处置组件、窗口页头、安装器及卸载显示，提供多尺寸 ICO 和原始透明 PNG。
- 提供图标来源、生成提示词与 ICO 重建脚本，保留已验收的文案、检测与处置行为，不扩大当前扫描范围。
- 记录下一阶段全部本地 Steam 创意工坊覆盖规划，从 Wallpaper Engine 单 App ID 扩展到多库、所有 App ID，详见 `docs/ROADMAP.md`。

## 0.1.9 - 2026-09-04

- 在边界与安全页面补充开源仓库、Apache License 2.0、fenglinbei 版权和第三方声明入口，保留当前扫描与处置文案，不增加测试标语。
- 构建脚本支持显式选择代码签名证书，签署自有 EXE/DLL、安装器和卸载器，再生成组件清单及发布哈希。第三方运行时原有签名不变。
- 同时附带公开证书和签名说明，不包含私钥，不自动导入证书信任。自签名构建不等于获得公开信任或 SmartScreen 信誉。
- 保留已验收 0.1.8 的管理员 Low Worker 启动修复，本版不改变检测规则和处置安全边界。

## 0.1.8 - 2026-09-04

- 修复管理员窗口启动受限扫描组件时的 `0xC0000142`：新建 Low 令牌的默认对象权限和所有者按当前用户配置，不再继承仅管理员可写的默认对象权限。不修改调用方令牌或既有目录权限。
- 保留原生进程句柄，稳定取得快速退出进程的十六进制退出码。启动失败、握手超时、非 Low 握手、异常退出及取消均有清理路径，错误输出有界保留并持续排空。
- 内容扫描失败时保留已完成的系统与 Steam 检查和报告导出，明确标记扫描不完整，不能当作完整干净复扫。错误说明区分启动阶段，不再一律提示是组件被安全软件删除。
- 不降低 Low Integrity、单进程 Job、安装权限、Broker 计划绑定或处置门槛。规则版本保持 2026.09.04.1，应用内仅显示版本号 0.1.8。
- 本机真实管理员启动已完成 Low 握手和无害文本/ZIP 扫描。测试范围不包含恶意样本执行、安装升级或隔离恢复闭环，详情见 `docs/WORKER-STARTUP-0.1.8.md`。

## 0.1.7 - 2026-09-04

- 修复安装 ACL 将 FullControl、Modify 组合值当成写权限掩码的问题，读取和执行不再误触发门禁，真实写入、删除、修改权限和取得所有权仍会拦截。
- 检查有效 ACE，正确处理仅继承规则、通用权限和 NULL DACL，覆盖自定义用户及组的写入授权。完整性校验覆盖清单中的文件及托管运行组件，清单本身和子目录也检查权限。
- 新增“打开管理员窗口”和“重新检查”，区分安装安全状态与当前权限。扫描默认无需提权，管理员账户的处置仍可自动请求 UAC，标准账户通过新窗口重新扫描后处置。
- UAC 取消、拒绝或启动失败时保留原窗口与报告，新窗口不接受旧报告、密码或处置计划，不放宽 Broker 的 SID 与哈希绑定。
- 安装检查移至后台，较小窗口的发现列表与详情按比例分配空间，详情可滚动。应用内仅显示版本号 0.1.7。
- 本地自动回归 141 项通过，原有密码和坏包回归继续通过。修复代码在未提权令牌下通过本机既有 Program Files 安装的只读检查，真实 UAC、标准用户输入另一账户凭据及处置闭环仍待安装验收。

## 0.1.6 - 2026-09-04

- 修复已成功验证的密码因其他条目超过限制而未进入复用缓存，普通成员读完、加密内容未验证、ZIP 校验失败时仍不缓存。
- 同一次扫描沿用上次选择的密码作用域，弹窗区分首次需要密码、已保存密码失败、刚输入密码失败和重复输入，长路径详情可展开。
- 按内容 SHA-256 合并已跳过或达到输入上限的加密内容，不在不同外层包里反复询问，相同内容仍保留覆盖缺口。之后新增的成功密码仍可自动尝试。
- 新增“重试未解密内容”，只重新扫描相关外层文件，清空旧会话密码并明确新结果不代表全机复扫。
- 对已识别的 ZIP、RAR、7z 使用对应解包器，保留密码错误信息，修复 AES ZIP 错密码变成未知格式异常的问题，坏包和不支持的归档只影响当前内容，不中止其他文件。
- 新增 ZIP AES、传统 ZIP 加密、RAR、7z 的独立测试及工作进程端到端回归，共 86 项通过。真实 25 文件目录的已知威胁数和可选处置数与 0.1.5 基线逐项一致。
- 规则版本保持 2026.09.04.1，未改变隔离权限边界和资源上限，未解密的内容仍不是安全结论。

## 0.1.5 - 2026-09-04

- 按成员内容增加恶意插件、客服篡改 DLL、加密凭据载荷及已复核可疑安装包规则，不将通用依赖、音效素材和分析文档一概判为病毒。
- 增加 UTF-8/UTF-16 组合特征识别，高置信度启发式发现可手动隔离，默认不勾选。
- 密码支持当前层、当前外层文件的嵌套内容、本次扫描全部压缩包三个作用域，仅缓存已成功解密的密码，有限重试且不重复发布失败尝试的结果。
- MP4 尾随归档进入受限递归检查，未展开的 MSI/复合文档明确标记覆盖缺口。
- 命中成员路径/哈希与外层隔离目标哈希分开记录，处置计划拒绝扫描后变化的文件，多成员命中仅隔离一次外层文件。
- 修复临时扩展名引起的误报、无文件名 GZip 路径误报、安全软件处理组件后错误提示不准确的问题，临时文件不再使用可执行扩展名。
- 应用内仅显示版本号，移除发行标语和开发待办，密码窗口支持滚动及较小窗口。
- 本地静态语料回归及无害自动化回归详见测试证据，未完成 Authenticode 签名与外部安全审计，仍按群内受控发布使用。

## 0.1.4 group preview - 2026-09-04

- 保留启发式发现的手动隔离能力，已知恶意项仍默认预选，启发式项默认不选。
- 新增 Program Files 安装器。便携运行可扫描和导出报告，管理员处置仅在 ACL 与三组件哈希校验通过后开放。
- ArchiveWorker 改为 Low Integrity 受限令牌，以挂起状态创建并在加入单进程 Job Object 后恢复，增加安全握手、精简环境变量和安装器网络阻断。
- Broker 绑定请求者 SID、计划 ID 与计划 SHA-256，计划校验与读取使用同一锁定句柄，结果只以不覆盖方式写入受保护固定目录，并增加管理员窗口二次确认。
- 文件隔离改为句柄绑定的“最终路径确认—哈希—复制—副本复核—原件删除”，目录隔离增加快照与指纹复核。
- Run/RunOnce 处置绑定注册表视图与扫描时值，计划任务改为精确名称与 XML 哈希匹配。
- Steam 前端处置前提示完整退出客户端，隔离异常前端/禁更配置后给出官方组件补全步骤。
- Steam 退出门禁只匹配 `steam.exe` 与 `steamwebhelper.exe`，不再误判 SteamSentinel 自身。
- 自动回归增加到 42 项。

当前版本仍未完成 Authenticode 签名、AppContainer 最小文件授权、外部安全审计和 Windows 多版本矩阵测试，仅用于群内受控测试。
