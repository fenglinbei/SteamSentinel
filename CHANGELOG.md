# Changelog

## 0.1.4 group preview - 2026-09-04

- 保留启发式发现的手动隔离能力，已知恶意项仍默认预选，启发式项默认不选。
- 新增 Program Files 安装器。便携运行可扫描和导出报告，管理员处置仅在 ACL 与三组件哈希校验通过后开放。
- ArchiveWorker 改为 Low Integrity 受限令牌，以挂起状态创建并在加入单进程 Job Object 后恢复，增加安全握手、精简环境变量和安装器网络阻断。
- Broker 绑定请求者 SID、计划 ID 与计划 SHA-256，计划校验与读取使用同一锁定句柄，结果只以不覆盖方式写入受保护固定目录，并增加管理员窗口二次确认。
- 文件隔离改为句柄绑定的“最终路径确认—哈希—复制—副本复核—原件删除”，目录隔离增加快照与指纹复核。
- Run/RunOnce 处置绑定注册表视图与扫描时值，计划任务改为精确名称与 XML 哈希匹配。
- Steam 前端处置前提示完整退出客户端，隔离异常前端/禁更配置后给出官方组件补全步骤。
- Steam 退出门禁只匹配 `steam.exe` 与 `steamwebhelper.exe`，不再误判 SteamSentinel 自身。
- 自动回归增加到 42 项。

当前版本仍未完成 Authenticode 签名、AppContainer 最小文件授权、外部安全审计和 Windows 多版本矩阵测试，仅用于群内受控测试。
