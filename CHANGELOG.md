# Changelog

## 0.1.19 - 2026-09-05

- 同版本密码交互修复：保留单密码与三种作用域，增加有序多密码候选及本次跳过所有未能解密的加密包；适用密码仍会尝试，未解密内容保留未检查记录。密码输入及重试预算继续受限，密码不进入日志、报告或命令行。详见 `docs/PASSWORD-0.1.19.md`。
- 密码输入框右侧增加显示/隐藏眼睛按钮，默认遮蔽；隐藏时清空明文框，候选列表始终遮蔽，提交、跳过与关闭均清理输入并恢复隐藏。
- 补齐目录加密在首个条目前的密码解码预算，每轮最多 512 次带密码尝试；失败历史使用有界的本轮带密钥指纹，避免批量失败密码重复解码。达到上限明确保留检查缺口。
- 同版本缺陷修复：扫描摘要区分部分检查、全部完成和未执行检查，明确补查建议；统一“检查范围说明”等文案，严重度不再等同于已确认病毒，密码提示明确仅在本次扫描中暂存。版本号、界面设计、扫描规则与处置权限不变。
- UI 视觉协调：统一表格单元格模板，使留白和垂直居中实际生效；单行最小高度从 40 调整为 32，长文本仍按需要换行并保留全文阅读。
- 风险、覆盖和隔离表的短字段、数字、日期与复选框按语义居中，说明和路径保持左对齐；修复覆盖页默认文字样式与自定义样式混用造成的错位。
- 保持 0.1.18 的页签、输入框、展开控件及原有圆角，不引入整套新主题；仅加强可用按钮的描边对比，禁用态使用独立灰色。
- 处置预览移除重复的单元格内留白；禁用表格 Delete 删除记录，避免浏览时从界面移走风险项。密码与长文本窗口保持 0.1.18 原设计。
- 新增低风险与覆盖记录的实际界面回归，并继续验证原小窗口、长文本、选择、滚动与只读安全边界。

## 0.1.18 - 2026-09-05

- UI 专项修复：移除主页面嵌套的整页滚动与固定最小内容高度，压缩顶栏，扫描入口、导出和当前页操作固定可见；小窗口自动收紧间距并折叠可选详情。
- 风险、覆盖、处置结果和隔离列表使用明确最小列宽与弹性列；长标题和路径提供摘要、悬停全文及可展开详情。预览移除导致列宽二次收缩的视觉分组，保留原动作顺序、批次与关联目标。
- 处置预览固定确认和执行区，完整计划说明独立页签；密码窗口固定继续/跳过入口，三种复用范围默认可见，补充说明局部展开。
- 新增真实 WPF 布局回归与独立 `--layout-ui` 入口：先显示空表再动态填充 92 条长路径记录，验证选中/滚动/调整尺寸，以及活动状态、覆盖与结果页、隔离列表和两个弹窗。
- 本版仅调整界面呈现与测试，不改变扫描规则、处置计划、权限门禁和密码复用语义。验证范围和边界见 `docs/COVERAGE-0.1.18.md`。

## 0.1.17 - 2026-09-05

- 修正 Quick 大文件、损坏魔数归档、排除路径及 Steam UI 截断枚举的覆盖状态；多根共享展开预算，错误密码只回滚逻辑计数，实际读取与元数据尝试始终有整轮上限，目录成员同样计入限额。
- PE 识别核验 DOS 偏移与 PE 签名，正常 Office/ZIP 容器不再仅因扩展名误报；增加域名边界、UTF-16BE 和内置规则结构校验。
- Broker 通过 HKLM 64 位受保护索引绑定事件身份与清单哈希，元数据按请求者 SID 只读、载荷仅管理员/SYSTEM 可读写；一次性 PlanId 结果通道与全局副作用锁防重放和并发。旧未索引事件不自动信任，未确认成功的动作不回滚，目录回滚暂时关闭。
- 受限 Worker 只继承协议句柄；父进程持有独立工作区并清理嵌套展开内容，支持取消控制消息与超时退出。修复进度节流丢弃快速阶段切换的问题。
- 附加检查失败保留既有发现；界面异常后进入只读恢复状态；管理员等待有界且超时不强杀处置进程。报告原子导出并记录构建身份，低分辨率窗口提供滚动访问和必要的辅助功能标签。
- 统一 `VersionPrefix`、程序集/文件版本和 `BuildIdentity` 来源；报告与发布证据记录完整提交、源码树和构建 ID，避免同版本二进制无法追溯。
- 固定 .NET SDK 10.0.400 与 NuGet 锁文件，新增 Windows CI：显式运行完整 SelfTest、保存机器可读结果，并强制 `passed >= 477`、`failed = 0`、`skipped = 0` 及版本身份一致。
- 发布流程拆分为 Preview 与 Release。产物先在同一输出根的唯一暂存目录完整构建与核验，再原子移动；已有同名目录一律拒绝覆盖。源码包改由 `git archive` 从实际构建快照生成，不再复制整个工作树。
- 正式发布要求干净工作树、`HEAD` 精确位于经过验证的 `v0.1.17` 签名注解标签，并使用公开受信的代码签名证书和 HTTPS RFC 3161 时间戳；缺少任一条件都会中止。预览包在文件名和清单中明确标识 `preview` / `dirty` 与签名状态。
- 安装器最低版本与应用目标统一为 Windows 10 2004（build 19041），文档按 `docs\` 原结构安装，并清理 0.1.17 前遗留在安装根目录的已知文档副本。
- 在 Broker 尚无可验证复扫证明链时，永久删除 fail closed：任何仍含活动记录的隔离事件（包括旧事件）都拒绝删除，只清理结构、路径均通过核验且所有记录已回滚的空事件；界面 Full clean 状态不作为授权证明。

## 0.1.16 - 2026-09-05

- 核验总读取额度提高到 4 GiB，保留 256 MiB 单文件限制和执行前独立身份校验。同批重复发现复用受只读锁保护的身份，批次完成、失败或取消后释放，不跨阶段缓存。
- 全选后按文件和依赖关系准备、预览并依次执行批次，每批仍不超过 64 个动作。共享宿主、入口和文件保持同组，单组超限明确列为未处理，身份变化、失败、取消授权或结果不确定时暂停后续批次。
- 新增逐项目标对账，展示未纳入原因、实际成功、失败和待核对结果，未纳入全部文件时不会只执行域名阻断后显示成功。
- 保留原始扫描范围与设置，处置后分别进行原内容范围复查和系统/Steam 复查，不再用系统配置提示替代原目录结果。原范围复查保留安全限额，2 分钟超时或取消后可手动复扫。
- 完整记录包保存原始选择、所有批次计划与结果，以及两种复查，不含样本或密码。详见 docs/COVERAGE-0.1.16.md。

## 0.1.15 - 2026-09-05

- 修复处置后复查在后台线程创建进度回调并直接更新 WPF 控件导致的闪退，扫描与补查统一使用显式窗口 Dispatcher，结束后的迟到更新会丢弃，密集更新合并交付。
- 新增固定忙碌状态栏，生成方案、等待确认、处置验证、复查、查询及导出分别说明阶段，展示旋转图标、流动条与本阶段已用时间，不伪造完成百分比，尊重系统减少动画设置。
- 将方案构建、管理员组件等待和报告导出中的耗时工作放在后台，处置开始后阻止直接关闭窗口，只读检查可取消后关闭。
- 增加有界本地错误日志和真实 WPF 消息循环回归，涵盖旧错误重现、后台进度、动画实际推进、回调失效、复查异常及取消。详见 docs/COVERAGE-0.1.15.md。

## 0.1.14 - 2026-09-04

- 重复覆盖记录按原因与补查根目录合并，保留准确累计次数和有限示例，修复大量长路径导致报告文本达到上限的问题。
- 完整内容扫描不再使用默认整轮 8 GiB 哈希额度，已识别关联落点优先检查，仍保留内存、文件数、解压与嵌套安全限制。
- 修复关联任务 XML 被 Windows 目录过滤误跳过、关联读取预算与其他系统扫描混用的问题。
- 处置前重新核对关联目标，必要时由受限扫描组件补查实际文件，按已核实的文件身份关联启动项、进程与隔离动作，保留启发式及独立管理员校验。
- 增加只读文件占用诊断、处置后两轮定向验证与系统/Steam 复查，区分动作执行和复查状态，不再复用处置前旧报告直接重试。
- 增加“进一步检查”、按关联目标分组的方案预览和不含样本的完整处置记录包。详细范围与验收见 docs/COVERAGE-0.1.14.md。

## 0.1.13 - 2026-09-04

- 文本特征改为分块分析与跨块信号累积，保留 UTF-8、UTF-16LE、家族组合特征和脚本启发式，AMSI 避免重复复制整个文件。
- 扫描结果在外层文件完成哈希绑定后分批交回，不再等待整轮结束后序列化巨大报告，JSON 导出也改为流式脱敏。
- 释放父压缩包解码器后再扫描内层，目录文件采用小批排序，结果量和内存接近安全上限时明确停止并保留已交回结果。
- 失败、异常退出和取消保留部分内容结果、最后路径、实际内部阶段、扫描设置和组件内存诊断，不再将内部失败统一标成等待组件退出。
- 保持 Low Integrity、1 GiB Job 限制、密码复用和原有处置校验，不改变已确认规则库。详见 docs/COVERAGE-0.1.13.md。

## 0.1.12 - 2026-09-04

- 分离扫描完成、风险发现和覆盖说明，覆盖记录不计入风险数量与最高严重度。
- 快速扫描优先启动型内容，正常 MP4 改为真实格式与顶层结构检查，保留尾随载荷与伪装检测，为小型启动文件预留独立哈希预算。
- 新增未检查内容分类与“使用完整内容扫描以覆盖此项”补查入口，说明加密、权限与安全上限不会自动解决。
- 常用操作保持可见，范围与高级选项折叠展示，保留真正读取失败、取消与组件故障的诊断信息。
- 详见 docs/COVERAGE-0.1.12.md。

## 0.1.11 - 2026-09-04

- 扩展至本地全部 Steam 工坊 AppID，加入首批游戏 MOD、Steam 插件和关联落点检查。
- 根据实际文件身份关联恶意组件与启动/运行证据，保留两个工具的查杀与只读分工。
- 新增内容范围、覆盖缺口和隐私说明，沿用正常 GUI 文案、图标与签名流程。
- 详细实现、静态回归与待实机验收事项见 docs/COVERAGE-0.1.11.md。

## 0.1.10 - 2026-09-04

- 新增蓝色盾牌与修复扳手应用图标，接入程序、扫描和处置组件、窗口页头、安装器及卸载显示，提供多尺寸 ICO 和原始透明 PNG。
- 提供图标来源、生成提示词与 ICO 重建脚本，保留已验收的文案、检测与处置行为，不扩大当前扫描范围。
- 记录下一阶段全部本地 Steam 创意工坊覆盖规划，从 Wallpaper Engine 单 App ID 扩展到多库、所有 App ID，详见 `docs/ROADMAP.md`。

## 0.1.9 - 2026-09-04

- 在边界与安全页面补充开源仓库、Apache License 2.0、fenglinbei 版权和第三方声明入口，保留当前扫描与处置文案，不增加测试标语。
- 构建脚本支持显式选择代码签名证书，签署自有 EXE/DLL、安装器和卸载器，再生成组件清单及发布哈希。第三方运行时原有签名不变。
- 同时附带公开证书和签名说明，不包含私钥，不自动导入证书信任。自签名构建不等于获得公开信任或 SmartScreen 信誉。
- 保留已验收 0.1.8 的管理员 Low Worker 启动修复，本版不改变检测规则和处置安全边界。

## 0.1.8 - 2026-09-04

- 修复管理员窗口启动受限扫描组件时的 `0xC0000142`：新建 Low 令牌的默认对象权限和所有者按当前用户配置，不再继承仅管理员可写的默认对象权限。不修改调用方令牌或既有目录权限。
- 保留原生进程句柄，稳定取得快速退出进程的十六进制退出码。启动失败、握手超时、非 Low 握手、异常退出及取消均有清理路径，错误输出有界保留并持续排空。
- 内容扫描失败时保留已完成的系统与 Steam 检查和报告导出，明确标记扫描不完整，不能当作完整干净复扫。错误说明区分启动阶段，不再一律提示是组件被安全软件删除。
- 不降低 Low Integrity、单进程 Job、安装权限、Broker 计划绑定或处置门槛。规则版本保持 2026.09.04.1，应用内仅显示版本号 0.1.8。
- 本机真实管理员启动已完成 Low 握手和无害文本/ZIP 扫描。测试范围不包含恶意样本执行、安装升级或隔离恢复闭环，详情见 `docs/WORKER-STARTUP-0.1.8.md`。

## 0.1.7 - 2026-09-04

- 修复安装 ACL 将 FullControl、Modify 组合值当成写权限掩码的问题，读取和执行不再误触发门禁，真实写入、删除、修改权限和取得所有权仍会拦截。
- 检查有效 ACE，正确处理仅继承规则、通用权限和 NULL DACL，覆盖自定义用户及组的写入授权。完整性校验覆盖清单中的文件及托管运行组件，清单本身和子目录也检查权限。
- 新增“打开管理员窗口”和“重新检查”，区分安装安全状态与当前权限。扫描默认无需提权，管理员账户的处置仍可自动请求 UAC，标准账户通过新窗口重新扫描后处置。
- UAC 取消、拒绝或启动失败时保留原窗口与报告，新窗口不接受旧报告、密码或处置计划，不放宽 Broker 的 SID 与哈希绑定。
- 安装检查移至后台，较小窗口的发现列表与详情按比例分配空间，详情可滚动。应用内仅显示版本号 0.1.7。
- 本地自动回归 141 项通过，原有密码和坏包回归继续通过。修复代码在未提权令牌下通过本机既有 Program Files 安装的只读检查，真实 UAC、标准用户输入另一账户凭据及处置闭环仍待安装验收。

## 0.1.6 - 2026-09-04

- 修复已成功验证的密码因其他条目超过限制而未进入复用缓存，普通成员读完、加密内容未验证、ZIP 校验失败时仍不缓存。
- 同一次扫描沿用上次选择的密码作用域，弹窗区分首次需要密码、已保存密码失败、刚输入密码失败和重复输入，长路径详情可展开。
- 按内容 SHA-256 合并已跳过或达到输入上限的加密内容，不在不同外层包里反复询问，相同内容仍保留覆盖缺口。之后新增的成功密码仍可自动尝试。
- 新增“重试未解密内容”，只重新扫描相关外层文件，清空旧会话密码并明确新结果不代表全机复扫。
- 对已识别的 ZIP、RAR、7z 使用对应解包器，保留密码错误信息，修复 AES ZIP 错密码变成未知格式异常的问题，坏包和不支持的归档只影响当前内容，不中止其他文件。
- 新增 ZIP AES、传统 ZIP 加密、RAR、7z 的独立测试及工作进程端到端回归，共 86 项通过。真实 25 文件目录的已知威胁数和可选处置数与 0.1.5 基线逐项一致。
- 规则版本保持 2026.09.04.1，未改变隔离权限边界和资源上限，未解密的内容仍不是安全结论。

## 0.1.5 - 2026-09-04

- 按成员内容增加恶意插件、客服篡改 DLL、加密凭据载荷及已复核可疑安装包规则，不将通用依赖、音效素材和分析文档一概判为病毒。
- 增加 UTF-8/UTF-16 组合特征识别，高置信度启发式发现可手动隔离，默认不勾选。
- 密码支持当前层、当前外层文件的嵌套内容、本次扫描全部压缩包三个作用域，仅缓存已成功解密的密码，有限重试且不重复发布失败尝试的结果。
- MP4 尾随归档进入受限递归检查，未展开的 MSI/复合文档明确标记覆盖缺口。
- 命中成员路径/哈希与外层隔离目标哈希分开记录，处置计划拒绝扫描后变化的文件，多成员命中仅隔离一次外层文件。
- 修复临时扩展名引起的误报、无文件名 GZip 路径误报、安全软件处理组件后错误提示不准确的问题，临时文件不再使用可执行扩展名。
- 应用内仅显示版本号，移除发行标语和开发待办，密码窗口支持滚动及较小窗口。
- 本地静态语料回归及无害自动化回归详见测试证据，未完成 Authenticode 签名与外部安全审计，仍按群内受控发布使用。

## 0.1.4 group preview - 2026-09-04

- 保留启发式发现的手动隔离能力，已知恶意项仍默认预选，启发式项默认不选。
- 新增 Program Files 安装器。便携运行可扫描和导出报告，管理员处置仅在 ACL 与三组件哈希校验通过后开放。
- ArchiveWorker 改为 Low Integrity 受限令牌，以挂起状态创建并在加入单进程 Job Object 后恢复，增加安全握手、精简环境变量和安装器网络阻断。
- Broker 绑定请求者 SID、计划 ID 与计划 SHA-256，计划校验与读取使用同一锁定句柄，结果只以不覆盖方式写入受保护固定目录，并增加管理员窗口二次确认。
- 文件隔离改为句柄绑定的“最终路径确认—哈希—复制—副本复核—原件删除”，目录隔离增加快照与指纹复核。
- Run/RunOnce 处置绑定注册表视图与扫描时值，计划任务改为精确名称与 XML 哈希匹配。
- Steam 前端处置前提示完整退出客户端，隔离异常前端/禁更配置后给出官方组件补全步骤。
- Steam 退出门禁只匹配 `steam.exe` 与 `steamwebhelper.exe`，不再误判 SteamSentinel 自身。
- 自动回归增加到 42 项。

当前版本仍未完成 Authenticode 签名、AppContainer 最小文件授权、外部安全审计和 Windows 多版本矩阵测试，仅用于群内受控测试。
